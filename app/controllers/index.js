const errorController = require('./error');
const notfoundController = require('./notFound');
const tokenController = require('./token');
const messagingController = require('./messaging');

module.exports = {errorController, notfoundController, messagingController, tokenController};